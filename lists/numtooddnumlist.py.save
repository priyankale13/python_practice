#!/usr/bin/python3

num=input("enter an integer:")
d=dict()
for n in num: 
    d[n]=d.get(n,0)+1
print(d)
lst=list(d.keys())
lst.sort(key=None,reverse=True)
print(lst)
maxnum=''.join(lst)
print(maxnum)
l1=[]
l2=[]
for i in range(1,int(maxnum)+1):
    if i%2==1
    l1.append(str(i))
print(l1)
"""
max=str(maxnum)
print(max)
for e in l1:
    list(e)
    for i in e:
        if i not in maxnum:
            break
    else:
        l2.append(e)
    
    #else:
        #l1.remove(e)
print(l2)
#print(l1)
"""
